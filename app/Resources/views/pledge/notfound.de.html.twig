{% extends 'base.html.twig' %}

{% block title %}Beitrag nicht gefunden{% endblock %}

{% block body %}
    <div class="row">
        <p class="text-danger">Der Gegenstand bzw. die Dienstleistung, die Du beitragen wolltest, wurde nicht gefunden.</p>

        <p>Bitte wähle einen anderen Beitrag:</p>

        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Beschreibung</th>
                    <th>Benötigt bis</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% if items is empty %}
                    <tr>
                        <td colspan="4">Alle benötigten Gegenstände und Dienstleistungen wurden bereits gespendet.</td>
                    </tr>
                {% else %}
                    {% for item in items %}
                    <tr>
                        {% if item.url is empty %}
                        <td>{{ item.name }}</td>
                        {% else %}
                        <td><a href="{{ item.url }}" title="{{ item.url }}" target="_blank">{{ item.name }}</a></td>
                        {% endif %}
                        <td>{{ item.description }}</td>
                        <td>{{ item.due|date("d.m.Y")}}</td>
                        <td><a href="{{ path('pledge', {'id': item.id}) }}"><i class="fa fa-gift" aria-hidden="true"></i> hier klicken, um zu spenden</a></td>
                    </tr>
                    {% endfor %}
                {% endif %}
            </tbody>
        </table>
    </div>
{% endblock %}
